
import './NotFound.module.less'

const NotFound = (props: any): JSX.Element => {
  console.log(props)
  return (
    <div styleName='NotFound'>
          NotFound
    </div>
  )
}

export default NotFound
