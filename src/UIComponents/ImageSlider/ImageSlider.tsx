
import './ImageSlider.module.less'

const ImageSlider = (props: any): JSX.Element => {
  console.log(props)
  return (
    <div styleName='ImageSlider'>
          ImageSlider
    </div>
  )
}

export default ImageSlider
