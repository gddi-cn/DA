// 应用场景
export enum ApplicationScene {
  // 终端
  ENDPOINT = 'endpoint',
  // 云端
  CLOUD = 'cloud',
  // 所有类型
  ALL = '-',
}

export enum ApplicationTemplateType {
  PUBLIC = 'public',
  PRIVATE = 'private'
}
