export namespace User {
  export namespace Consume {
    export enum Type {
      ALL,
      CALCULATE,
      AUTH,
      CHANNEL,
      MODEL_TRAIN,
      EDGE_DEVICE_REGISTER,
      TERMINAL_DEVICE_REGISTER,
    }
  }
}
